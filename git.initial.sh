# 1. Initialize a new Git repository in the current directory
git init

# 2. Add all current files to the staging area
git add .

# 3. Create the first commit (a snapshot of your current working code)
git commit -m "Initial commit of Flask assignment application"

# 4. (Optional but recommended) Rename the default branch to 'main'
git branch -M main
